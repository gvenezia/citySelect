<% include partials/header %>

<div class="container">
    
    <div class="row text-center" style="display: flex; flex-wrap: wrap">
        <div class="col-sm-10 justify-content-cente">
            <h2><%= city.name %></h2>
            
            <figure class="figure">
              <img src="<%= city.image %>" class="figure-img img-fluid rounded" alt="A picture of <%= city.name %>">
              <figcaption class="figure-caption font-weight-bold"> <%= city.description %> </figcaption>
            </figure>
        <!--Loop for the comments-->
        <% city.comments.forEach(function(comment){ %>
            <p>
                "<%= comment.text %>" <span> — <%= comment.author %></span>
            </p>
            
        <% }); %>
                
        </div>
    </div>
    
</div>  <!-- End container -->

<% include partials/footer %>